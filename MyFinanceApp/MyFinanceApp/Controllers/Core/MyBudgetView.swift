import SwiftUI

struct MyBudgetView: View {
    var body: some View {
        Text(/*@START_MENU_TOKEN@*/"Hello, World!"/*@END_MENU_TOKEN@*/)
            .padding()
        Button(action: {}) {
            Text("Tap me")
        }
    }
}

#Preview {
    MyBudgetView()
}
